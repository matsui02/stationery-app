<header class="px-[50px] py-6">
  <% if !user_signed_in? %>
  <div class="flex items-center justify-between">
    <%= link_to "Pencase", root_path, class: "font-noto-serif text-[40px]" %>
    <div class="flex items-center gap-5">
      <%= link_to "新規登録", new_user_session_path, class: "sign-up-btn" %> <%= link_to "ログイン", "#", class: "sign-in-btn" %> </div>
  </div>
  <% else %>
  <% end %>
</header>
